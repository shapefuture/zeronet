# ...main.yml...
      - name: Archive build
        uses: actions/upload-artifact@v4
        with:
          name: web-build
          path: apps/web-app/.next

      - uses: actions/download-artifact@v4
        with:
          name: web-build
          path: .next
# Repeat as needed for every `upload-artifact` and `download-artifact`.